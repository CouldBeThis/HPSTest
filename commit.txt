Troubleshooting [05] 

* change = plain text post should NOT be a draft
* all else the same

config.toml:
  baseURL = "https://couldbethis.github.io/HPStest"
  theme = "ananke" # unchanged from previous

hugophotoswipe.yml:
	markdown_dir: ../../docs/galleries/
	output_dir: ../../static/pics/
output_format: jpg
photo_dir: photos
smartcrop_js_path:
	url_prefix: /HPSTest/pics/

command run from hugo root to build:

trash docs && trash static/pics && cd src/photos && hps clean && hps --fast update && cd ../.. && hugo && git st && git add -A && git commit -F "commit.txt" && git push
