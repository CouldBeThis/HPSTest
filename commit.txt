Troubleshooting [17]

* layouts/index.html: remove hard-coded CSS in <main class="container"> 
* add a bunch of emojis to facilitate troubleshooting

  config.toml:
    baseURL = "https://couldbethis.github.io/HPSTest/"
    theme = "ananke"

    hugophotoswipe.yml:
            markdown_dir: ../../content/galleries/
            output_dir: ../../static/pics/
    output_format: jpg
    photo_dir: photos
    smartcrop_js_path:
            url_prefix: /HPSTest/pics/

    command run from hugo root to build:

echo $(date) > content/posts/date.html && trash docs && trash static/pics && cd src/photos && hps clean && hps update && cd ../.. && hugo && git st && git add -A && git commit -F "commit.txt" && git push
