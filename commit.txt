Troubleshooting [09] 

* change text of my-first-post again

config.toml:
  baseURL = "https://couldbethis.github.io/HPStest/"
  theme = "ananke" # unchanged from previous


command run from hugo root to build:

trash docs && hugo && git st && git add -A && git commit -F "commit.txt" && git push && git log
